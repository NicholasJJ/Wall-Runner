using UnityEngine;
using System.Collections;

public class activateSpeed : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
//	void Update () {
//		if(GetComponentInParent<WRmove>().isHookTraveling){
//			Debug.Log("trying to activate speed");
//			GetComponent<ParticleSystem>().startSize = 0.5f;
//		} 
//		else {
//			GetComponent<ParticleSystem>().startSize = 0f;
//		}
//	}
}
